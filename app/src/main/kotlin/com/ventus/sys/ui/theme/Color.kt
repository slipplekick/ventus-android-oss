package com.ventus.sys.ui.theme

import androidx.compose.ui.graphics.Color

// Ported directly from the desktop app's CSS custom properties
// (assets/css/style.css) so the Android app looks like the same product,
// not a reskin — same dark/neon aesthetic, same named roles.
val VentusVoid = Color(0xFF010306) // --void
val VentusSurface = Color(0xFF070D18) // --surface
val VentusPanel = Color(0xFF0A1220) // --panel
val VentusPanel2 = Color(0xFF0D1728) // --panel2
val VentusText = Color(0xFFCDE4F8) // --text
val VentusIce = Color(0xFFA8D4FF) // --ice
val VentusQuincy = Color(0xFF4DB8FF) // --quincy (primary accent)
val VentusNeon = Color(0xFF00E5CC) // --neon
val VentusBankai = Color(0xFFFF4C2B) // --bankai (error/alert)
val VentusChrome = Color(0xFFF5E642) // --chrome (warning/gold)
